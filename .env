# Disable source maps. Avoids source-map-loader warnings from dependencies
# that ship compiled JS without their original TypeScript sources
# (e.g. @microsoft/fetch-event-source). Applies to both `start` and `build`.
GENERATE_SOURCEMAP=false
